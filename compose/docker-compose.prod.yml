services:
  mongodb:
    image: mongo:7
    container_name: travion-mongodb-prod
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: travion
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ../scripts/backup:/backup
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 30s
      timeout: 10s
      retries: 5

  backend:
    image: ghcr.io/travion-reserch-project/travion-backend:latest
    container_name: travion-backend-prod
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - ../.env.backend
    environment:
      - NODE_ENV=production
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M

  frontend:
    image: ghcr.io/iamsahan/the-travion:latest
    container_name: travion-frontend-prod
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://traviongo.online/api
    depends_on:
      - backend
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  transport-service:
    image: ${TRANSPORT_SERVICE_IMAGE:-ghcr.io/travion-reserch-project/transport-service:latest}
    container_name: travion-transport-service-prod
    restart: always
    ports:
      - "8001:8001"
    env_file:
      - ../.env.transport-service
    environment:
      - PYTHON_ENV=production
    volumes:
      - transport_models:/app/ml-models
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  safety-service:
    image: ${SAFETY_SERVICE_IMAGE:-ghcr.io/travion-reserch-project/safety-service:latest}
    container_name: travion-safety-service-prod
    restart: always
    ports:
      - "8003:8003"
    env_file:
      - ../.env.safety-service
    environment:
      - PYTHON_ENV=production
    volumes:
      - safety_models:/app/model
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  weather-service:
    image: ${WEATHER_SERVICE_IMAGE:-ghcr.io/travion-reserch-project/weather-service:latest}
    container_name: travion-weather-service-prod
    restart: always
    ports:
      - "8002:8002"
    env_file:
      - ../.env.weather-service
    environment:
      - PYTHON_ENV=production
    volumes:
      - weather_models:/app/models
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  ai-agent-engine:
    image: ${AI_AGENT_ENGINE_IMAGE:-ghcr.io/travion-reserch-project/ai-agent-engine:latest}
    container_name: travion-ai-agent-engine-prod
    restart: always
    ports:
      - "8004:8004"
    env_file:
      - ../.env.ai-agent-engine
    environment:
      - PYTHON_ENV=production
    volumes:
      - ai_models:/app/models
      - ai_data:/app/data
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  mcp-google-maps:
    image: ${MCP_GOOGLE_MAPS_IMAGE:-ghcr.io/travion-reserch-project/mcp-google-maps:latest}
    container_name: travion-mcp-google-maps-prod
    restart: always
    ports:
      - "8010:8010"
    env_file:
      - ../.env.mcp-google-maps
    environment:
      - PYTHON_ENV=production
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    build:
      context: ../docker/nginx
    container_name: travion-nginx-prod
    restart: always
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - transport-service
    volumes:
      - ../docker/nginx/ssl:/etc/nginx/ssl:ro
      - nginx_logs:/var/log/nginx
      - certbot_data:/var/www/certbot:ro
    networks:
      - travion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  certbot:
    image: certbot/certbot:latest
    container_name: travion-certbot
    volumes:
      - ../docker/nginx/ssl:/etc/letsencrypt
      - certbot_data:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - travion-net

  dozzle:
    image: amir20/dozzle:latest
    container_name: travion-dozzle
    restart: always
    ports:
      - "8888:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - travion-net

  prometheus:
    image: prom/prometheus:latest
    container_name: travion-prometheus
    restart: always
    ports:
      - "9090:9090"
    volumes:
      - ../monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
    networks:
      - travion-net

  grafana:
    image: grafana/grafana:latest
    container_name: travion-grafana
    restart: always
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SNAPSHOTS_EXTERNAL_ENABLED=true
      - GF_SNAPSHOTS_EXTERNAL_SNAPSHOT_URL=https://snapshots.raintank.io
      - GF_SNAPSHOTS_EXTERNAL_SNAPSHOT_NAME=Publish to snapshots.raintank.io
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - travion-net

  node-exporter:
    image: prom/node-exporter:latest
    container_name: travion-node-exporter
    restart: always
    ports:
      - "9100:9100"
    command:
      - "--path.rootfs=/host"
    pid: host
    volumes:
      - "/:/host:ro,rslave"
    networks:
      - travion-net

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: travion-cadvisor
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    privileged: true
    networks:
      - travion-net

volumes:
  mongodb_data:
  transport_models:
  safety_models:
  weather_models:
  ai_models:
  ai_data:
  nginx_logs:
  certbot_data:
  backend_logs:
  portainer_data:
  prometheus_data:
  grafana_data:

networks:
  travion-net:
    driver: bridge
